block author
    - var image = model.image;
    - var name = model.name;

    if author
        .author
            if image
                - var url = image.url;
                - var alt = image.alt;

                span.author__pic
                    img(src=url, alt=alt)

            span= 'By '
            span.author__name!= name
