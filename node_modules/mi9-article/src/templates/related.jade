a(class="related heading-link", href=model.url, data-tracking-link-name=model.trackingLinkName)
    if model.media
        .related__image
            != partials.imageResizer({model: model.media})
        .related__heading
            != model.media.headline
